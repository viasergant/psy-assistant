package com.psyassistant.riskflags.domain;

/** Action types recorded in the {@link RiskFlagAuditLog}. */
public enum RiskFlagAuditActionType {
    FLAG_CREATED,
    FLAG_RESOLVED,
    FLAG_UPDATED
}

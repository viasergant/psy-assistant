package com.psyassistant.sessions.domain;

/**
 * Classifies the type of follow-up task associated with a session.
 */
public enum FollowUpTaskType {
    /** Follow up with a client who did not attend their scheduled session. */
    NO_SHOW_FOLLOW_UP
}
